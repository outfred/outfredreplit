🎯 الهدف

تمكين التجّار من استيراد منتجاتهم إلى Outfred بثلاث طرق:

Shopify (Storefront/Admin token)

WooCommerce (REST API keys)

CSV (رفع ملف)

خيار رابع اختياري: من روابط صفحات المنتجات (HTML/JSON-LD) لما مفيش API.

مع لوحة إعدادات في الأدمن للتحكم في المفاتيح الافتراضية، حدود الاستيراد، الجدولة، والـ rate limits.

👤 صلاحيات وأمان

التاجر: يرى “استيراد” داخل Merchant Dashboard فقط لمنتجات متجره.

الأدمن/المالك: يرى إعدادات عامّة في Admin Dashboard، ويقدر يعمل Import بالنيابة عن تاجر محدّد.

RBAC:

/merchant/import/* → merchant|owner

/admin/import/* → admin|owner (مع اختيار merchantId المستهدف)

Rate limits: حدّ يومي/بالساعة لكل تاجر (مثلاً 3 مرات/ساعة، 10 مرات/يوم).

Logs/Metrics: سجّل كل عملية (بداية/نجاح/فشل/عدد مُنشأ/مُحدَّث/زمن التنفيذ).

🧭 تدفّقات الاستخدام (UX Flows)
1) لوحة التاجر → تبويب “استيراد”

Sections داخل تبويب واحد “Import”:

Shopify

حقول: Base URL, Storefront Token + (اختياري Admin token)

اختيار عدد المنتجات (limit 50–250)

زر Validate (يتأكد من الوصول) → يعرض “Preview” (أول 10 عناصر) → زر Import

WooCommerce

حقول: Base URL, Consumer Key, Consumer Secret

نفس تدفّق Validate → Preview → Import

CSV

Upload → Preview أوّل 20 صف → Field Mapping (مثال: title, description, price, currency, colors, sizes, tags, images, brand, city, gender, fit, sku) → Validate → Import

From URLs (اختياري)

Textarea للروابط (واحدة/متعدّدة)، حقل brand وcity → Validate (يجلب عيّنات) → Import

نتائج العملية (Summary):

created, updated, skipped, errors[]

زر Download Report (JSON/CSV)

تحكّم الفهرسة (Indexing):

Switch: “Reindex after import” (On/Off)

زر “Reindex now”

Toasts/رسائل (EN/AR):

EN: “Validated: 97 products found. Preview loaded.”

AR: “تم التأكيد: تم العثور على ٩٧ منتجًا. تم تحميل المعاينة.”

EN: “Imported 62 created, 15 updated, 3 skipped.”

AR: “تم الاستيراد: ٦٢ عنصر جديد، ١٥ تحديث، ٣ تم تخطيها.”

2) لوحة الأدمن → “Global Import Settings”

Tabs:

Defaults:

حدود الاستيراد (per run / per hour / per day)

أقصى حجم CSV، أقصى صور للمنتج

سياسة الصور (WebP/أصلي)

Shopify / Woo defaults:

مفاتيح افتراضية (إن وُجدت) تُشحن على الجهة الخلفية مشفّرة في SystemConfig

خيارات page size / retries / backoff

Scheduling:

جدولة قائمة Merchants لتشغيل Import تلقائي (كل 6 ساعات مثلاً)

عرض الحالة (آخر تشغيل، المدة، عدد النجاحات، عدد الإخفاقات)

Run as:

اختيار Merchant → تشغيل Import بنوعه (Shopify/Woo/CSV/URLs)

نتائج مفصّلة + Download report

Rate Limits & Audit:

عرض الاستهلاك/الحدود لكل تاجر اليوم/الأسبوع

سجل عمليات آخر 30 يومًا (التاجر/النوع/المدّة/النتيجة)

🔁 قواعد الدمج (Mapping & Dedup)

مفتاح عدم التكرار: (merchantId, title, color, size)

لو مفيش color/size، استخدم default/onesize.

الصور:

لو Import CSV/API: خزّن الروابط كما هي مبدئيًا.

اختياري: تنزيل إلى السيرفر وتحويل WebP (تتحكم فيها إعدادات الأدمن).

الأسعار: price إلى priceCents = round(price * 100)، العملة “EGP” افتراضيًا لو غير محددة.

Brand:

لو Shopify/Woo: vendor/brand → upsert في جدول Brand.

لو CSV/URLs: استخدم الحقل أو “Unknown” مع إمكانية تعديله لاحقًا.

الحالة: جميع العناصر تُعلَّم published=true افتراضيًا (يُضبط من الإعدادات).

🧪 التحقق/التصديق (Validation)

Shopify/Woo: زر Validate قبل الاستيراد:

يتأكد من صحة المفاتيح والوصول

يرجّع عدّاد العناصر + عيّنة Preview (10)

CSV:

تأكيد الأعمدة المطلوبة (title, price على الأقل)

تحويل القيم (trim/normalize)، أسعار أرقام، الصور Array

URLs:

اختبار استجابة 200

استخراج JSON-LD/OpenGraph للعناوين/الصور، وإلّا سحب أوّل h1 وأوّل صورة

🖼️ سياسة الصور وتوفير المساحة

بحث بالصورة (موجود عندك مسبقًا): uploads/tmp + TTL 10 دقائق.

صور المنتجات (دائم):

uploads/products/{merchantId}/{productId}/

أسماء content-hash لتفادي التكرار

إعداد اختياري لتحويل WebP وتقليل المقاس (تُضبط من الأدمن)

حذف ملفات المنتج عند حذف المنتج

🧰 فهرسة وإعادة البناء (Indexing)

بعد الاستيراد:

لو Switch “Reindex after import” مفعّل → شغّل Job الفهرسة على المنتجات المستوردة فقط (دفعات 100).

الأدمن يقدر يعمل “Reindex all” يدويًا من لوحة النظام.

مقاييس: سجّل زمن الفهرسة وعدد العناصر/فشل.

📊 القياس والمتريكس

سجّل لكل عملية:

merchantId, type (shopify/woo/csv/urls)

requested, created, updated, skipped, errors

tookMs, p95, p99 (لو متاح)

rateLimitRemaining (لو مفعل)

اجعلها ظاهرة في Admin → Metrics/Imports.

⚖️ قانوني وأخلاقي

احترام robots.txt وسياسات المواقع في “From URLs”.

استخدام الاستيراد الرسمي (Shopify/Woo) عند الإمكان.

وضع User-Agent واضح.

Rate limit قوي على الاستيراد من المواقع الخارجية.

✅ معايير القبول (Acceptance)

تاجر يفتح “Import” → يختار Shopify/Woo/CSV/URLs → Validate → Preview → Import.

تظهر نتيجة مفصّلة: created/updated/skipped/errors + Download report.

المنتجات تظهر فورًا في GET /api/products، وتتحدّث في Dashboard.

Switch “Reindex after import” يعمل ويؤثر على البحث (text/image).

Admin يقدر يجدول Import ويشوف الحالة/السجل والحدود.

رسائل التوست ثنائية اللغة (AR/EN) + RTL/LTR صح.

🧭 نصوص واجهة مقترحة (AR/EN)

Validate:

EN: “Connected. Found 134 products. Preview ready.”

AR: “تم الاتصال. تم العثور على ١٣٤ منتجًا. المعاينة جاهزة.”

Import started:

EN: “Import started. Please stay on this page.”

AR: “بدأ الاستيراد. يُفضَّل البقاء على هذه الصفحة.”

Import result:

EN: “Imported 70 new, 22 updated, 5 skipped.”

AR: “تم الاستيراد: ٧٠ جديد، ٢٢ تحديث، ٥ تم تخطيها.”

Error:

EN: “Validation failed. Please check credentials or mapping.”

AR: “فشل التحقق. من فضلك راجع بيانات الدخول أو المطابقة.”

🧩 ما الذي تنفّذه أنت يا Replit (قائمة مهام)

إضافة تبويب Import في Merchant Dashboard بهذه الأقسام والأزرار والحوارات (Validate/Preview/Import/Report).

إضافة صفحة Admin → Global Import Settings بعناصر التحكم والجدولة والسجلات.

توصيل الأزرار بمسارات الـ API الحالية/الجديدة (أسماء المسارات كما بالشرح؛ الالتزام بالـ RBAC).

تطبيق قواعد Mapping/Dedup والـ Rate limits وMetrics الموصوفة.

التأكد من i18n (AR/EN) والـ RTL/LTR في كل الشاشات والحوارات.

إظهار toasts واضحة، وحالات فشل 503 عند إغلاق مزوّدات أو فشل التح